/* @flow */
import LogoType from "../icons/LogoType"
import styled from "styled-components"

const BrimTextLogo = styled(LogoType)`
  filter: drop-shadow(0 1px 2px rgba(0, 0, 0, 0.2));

  svg {
    display: block;
    margin: 0 auto;
  }
`

export default BrimTextLogo
