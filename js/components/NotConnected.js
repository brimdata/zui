import React from "react"

const NotConnected = () => (
  <div className="not-connected">
    <p>Having trouble connecting to boomd. Retrying in the background.</p>
  </div>
)

export default NotConnected
