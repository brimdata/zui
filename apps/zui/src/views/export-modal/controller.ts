export class ExportModalController {
  // add default for zng or the last most recent format
  constructor(public close: any) {}

  submit(e) {
    e.preventDefault()
    // if (toPool)
    // exportToPool(data)
    // elseif (toClipboard)
    // else
    // exportToFile(data)
    // const form = e.currentTarget
    // const data = new FormData(form)
    // const pool = getPoolFormData(form)
    // this.toPool()
  }
}
