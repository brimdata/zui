{
  "name": "zealot",
  "version": "0.0.0",
  "main": "dist/index.js",
  "module": "dist/index.es.js",
  "types": "dist/index.d.ts",
  "author": "Brim Security, Inc.",
  "license": "ISC",
  "scripts": {
    "build": "npm-run-all clean build:*",
    "build:types": "node ../node_modules/typescript/bin/tsc -p tsconfig.json -outDir dist --emitDeclarationOnly -d",
    "build:js": "rollup -c",
    "test": "npm-run-all test:*",
    "test:api": "cd test_api && deno test --allow-net --allow-run --allow-read --allow-write",
    "test:unit": "node ../node_modules/jest/bin/jest.js src",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^6.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "rollup": "^2.28.2"
  }
}
